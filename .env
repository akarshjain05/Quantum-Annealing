# Used by docker-compose.yml. Copy to .env before `docker compose up --build`.

SECRET_KEY=dev-only-secret-CHANGE-ME-before-any-real-deployment

# Optional - the agent is fully functional without these (see docs/agent-architecture.md)
LLM_PROVIDER=
ANTHROPIC_API_KEY=
